{
  "version": 2,
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @workspace/scholar-guide run vercel-build",
    "outputDirectory": "dist",
  
  "framework": null,
  "crons": [
    {
      "path": "/api/cron/renew-scholarships",
      "schedule": "0 0 * * *"
    }
  ]
}
